{
  "name": "freestruct",
  "version": "0.1.0",
  "description": "SEO layer for static doc sites",
  "scripts": {
    "build": "jekyll build && npm run inject",
    "inject": "node docs/lib/inject.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.3"
  }
}
